# Minimal dependencies for the deployed API server only (app/) --
# deliberately excludes the ML pipeline's heavy geospatial/scientific
# stack (rasterio, geopandas, scikit-learn, etc. -- see requirements.txt
# for the full local-dev set), since none of it is imported by app/.
# Keeps the deployed image small and the build fast/reliable on a
# free-tier host.
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
geoalchemy2==0.16.0
shapely==2.1.2
psycopg2-binary==2.9.10
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.32
httpx==0.28.1
python-dotenv==1.0.1
